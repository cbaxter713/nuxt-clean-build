{
  "name": "nuxt-clean-build",
  "version": "1.0.0",
  "description": "My excellent Nuxt.js project",
  "author": "Colin Baxter",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "git push heroku master",
    "heroku-postbuild": "npm run build"
  },
  "dependencies": {
    "cross-env": "^5.2.0",
    "nuxt": "^2.4.0"
  },
  "devDependencies": {
    "nodemon": "^1.18.9"
  }
}
